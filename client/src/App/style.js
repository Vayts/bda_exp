import styled from 'styled-components';

export const AppWrapper = styled.div`
	position: relative;
	// max-height: ${({ modal }) => (modal ? '100vh' : 'auto')};
	// overflow: ${({ modal }) => (modal ? 'hidden' : 'auto')};
`;
