import styled from 'styled-components';
import { COLORS } from '@constants/colors';

export const ExitModalWrapper = styled.div`
	padding: 10px 0;
`;

export const ExitModalTitle = styled.h3`
	text-align: center;
	font-size: 20px;
	margin-bottom: 40px;
	color: ${COLORS.light.primary};
`;

export const ExitModalButtons = styled.div`
`;
